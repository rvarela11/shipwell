// @vendors
import React from 'react';

// @styles
import './Header.scss';

const Header = () => (
    <header className="header">
        <div className="header__logo" />
    </header>
);

export default Header;
